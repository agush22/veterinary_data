<!DOCTYPE html>
<html>
<head>
  <title>Vet</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <ul class="hmenu">
  <% if customer_signed_in? %>
    <li>
      <%= link_to('Logout', destroy_customer_session_path, :method => :delete) %>
    </li>
  <% elsif admin_signed_in? %>
    <li>
      <%= link_to('Logout', destroy_admin_session_path, :method => :delete) %>
    </li>
  <% elsif doctor_signed_in? %>
    <li>
      <%= link_to('Logout', destroy_doctor_session_path, :method => :delete) %>
    </li>
  <% elsif receptionist_signed_in? %>
    <li>
      <%= link_to('Logout', destroy_receptionist_session_path, :method => :delete) %>
    </li>
  <% end %>
  </ul>

<%= yield %>

</body>
</html>
