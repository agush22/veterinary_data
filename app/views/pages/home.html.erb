<h1>WELCOME TO THE VET!</h1>
<ul class="hmenu">
  <% if !customer_signed_in? && !admin_signed_in? && !doctor_signed_in? %>
    <li>
      <ul>
        <li><%= link_to "Customer Sign In", new_customer_session_path%></li>
        <li><%= link_to "Admin Sign In", new_admin_session_path%></li>
        <li><%= link_to "Doctor Sign In", new_doctor_session_path%></li>
        <li><%= link_to "Receptionist Sign In", new_receptionist_session_path%></li>
      </ul>
    </li>
  <% end %>
</ul>
